#### environment
.env
node_modules/
.eslintcache
# Ignore the contents, not the directory itself — git cannot re-include a
# path whose parent directory is excluded, so `.scratch/` here would make
# the `!` line below dead and leave `.scratch/` absent from a fresh clone.
.scratch/*
!.scratch/directory-keeper

#### implementation repositories, fetched or adopted by /hora-setup
/*-backend*/
/*-frontend*/

# A repository adopted under its own directory name matches neither pattern
# above, so /hora-setup appends one literal entry per declared `Directory`
# here. Left out, the whole repository gets committed into this one, and
# nothing says so until somebody reads `git status`.

#### skills equipped by equip-skills.sh from the conventions package (regenerated, not authored here)
#
# Ignore everything under .claude/skills/ and name this repository's own back
# in. A conventions package's skill names belong to that package, and a
# denylist that stops matching says nothing when it stops — every equipped
# skill would simply start being committed here. An allowlist of the ones we
# author cannot fail that way.
#
# equip-skills.sh reads this list through `git check-ignore`, so a skill added
# here without being named below is treated as the package's and removed on
# the next equip.
/.claude/skills/*
!/.claude/skills/bank-id/
!/.claude/skills/hora/
!/.claude/skills/hora-accept/
!/.claude/skills/hora-build/
!/.claude/skills/hora-plan/
!/.claude/skills/hora-setup/
!/.claude/skills/hora-spec/
!/.claude/skills/hora-spec-usecases/
!/.claude/skills/hora-spec-horizon/
!/.claude/skills/hora-spec-nonfunctional/
!/.claude/skills/hora-spec-provider/
!/.claude/skills/hora-spec-consumer/
!/.claude/skills/hora-spec-security/
!/.claude/skills/hora-spec-review/

#### for Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db

#### for Mac
.DS_Store

#### environment to develop
.vscode/*
!.vscode/cspell.json
!.vscode/settings.json

#### Lock files
yarn.lock
pnpm-lock.yaml
bun.lockb
